; Created by Nestor Gramata Jr. 
; Edited 20171115
; Lines that begin with semi-colon are comments
; 3 variables are declared here
;  1. smtpServer -> the virtual SMTP server's IP address
;  2. emailFrom -> the sender's address
;  3. emailTo ->  the recipient's address (can be several)
; No unnecessary whitespaces must be placed on either side of the variable
; The lines starts with a variable name, no whitespace
; Parser ignores lines starting with space
; Parser ignores blank lines
; Variable names can have whitespaces (be careful)
; Sample Format and Variables are Declared as Follows:
;
; <variable>=<value>
;
; smtpServer=192.168.1.1
; emailFrom=user1@mailer.com
; emailTo=user2@mailer.com, user3@mailer.com
; 

smtpServer=10.164.91.22
emailFrom=PeopleSoft@peoplesoft.com
emailTo=nestor.b.gramata.jr@accenture.com
